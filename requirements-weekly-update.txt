# Shared runtime-only dependencies for active-season Update League workers.
# Provider clients live in the platform-specific files so a Sleeper update
# never installs Yahoo or ESPN packages (and vice versa).
duckdb==1.5.5
pandas==2.2.3
pyarrow==15.0.2
requests==2.32.3
numpy==1.26.4
polars==1.35.2
