name: Sleeper Full Import Worker (All Years)

# This workflow imports ALL years including current year to run complete transformations.
# Quick import fetches raw data; full import runs transformations on the complete dataset.
# Called from: trigger-downstream action after sleeper_quick_import_worker.yml completes.

on:
  workflow_dispatch:
    inputs:
      league_data_b64:
        description: 'Base64-encoded JSON with Sleeper league configuration'
        required: false
        type: string
      user_id:
        description: 'Unique identifier for this user/import job'
        required: true
        type: string
      import_lock_key:
        description: 'Stable import concurrency key, usually the resolved db_name'
        required: false
        type: string
      verify_only:
        description: 'Run the complete local pipeline without publishing or registering the league'
        required: true
        type: boolean
        default: false
  repository_dispatch:
    types: [sleeper_full_import]


permissions:
  actions: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.inputs.import_lock_key || github.event.client_payload.import_lock_key || github.event.inputs.user_id || github.event.client_payload.user_id || github.run_id }}
  cancel-in-progress: false

jobs:
  resolve-db-lock:
    runs-on: ubuntu-latest
    outputs:
      database_name: ${{ steps.resolve.outputs.database_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /.github/
            /fantasy_football_data_scripts/
            /scripts/
            /requirements.txt
            /pyproject.toml

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Resolve lock key
        id: resolve
        run: |
          python .github/scripts/resolve_sleeper_workflow_input.py
        env:
          LEAGUE_DATA_B64: ${{ github.event.inputs.league_data_b64 || github.event.client_payload.league_data_b64 }}
          IMPORT_MODE: full
          DATABASE_BACKEND: fly
          DATABASE_SERVER_URL: ${{ secrets.DATABASE_SERVER_URL }}
          DATABASE_READ_TOKEN: ${{ secrets.DATABASE_READ_TOKEN }}

  import-sleeper-history:
    needs: resolve-db-lock
    runs-on: ubuntu-latest
    timeout-minutes: 90  # Historical imports may take longer

    env:
      PYTHONUNBUFFERED: 1
      IMPORT_PIPELINE_V2: ${{ vars.IMPORT_PIPELINE_V2 }}
      DATABASE_BACKEND: fly
      DATABASE_SERVER_URL: ${{ secrets.DATABASE_SERVER_URL }}
      DATABASE_READ_TOKEN: ${{ secrets.DATABASE_READ_TOKEN }}
      DATABASE_ADMIN_TOKEN: ${{ secrets.DATABASE_ADMIN_TOKEN }}
      FLY_PRIMARY_MACHINE_ID: ${{ secrets.FLY_PRIMARY_MACHINE_ID }}
      FLY_UPLOAD_MAX_RETRIES: 40
      FLY_UPLOAD_RETRY_BASE_DELAY: 3
      FLY_UPLOAD_RETRY_MAX_DELAY: 30
      FLY_UPLOAD_RETRY_JITTER_SECONDS: 2
      FLY_PUBLISH_FORMAT: delta
      FLY_DELTA_FALLBACK_TO_DUCKDB: '0'
      FLY_FINALIZE_INVENTORY: '1'
      OPS_CACHE_PATH: ${{ github.workspace }}/research_public_lake/ops_cache.duckdb
      VERIFY_ONLY: ${{ github.event_name == 'workflow_dispatch' && inputs.verify_only || false }}
      DB_NAME: ${{ github.event.inputs.league_data_b64 && '' || '' }}  # set after parse step
      DATA_DIR: ""  # set after parse step

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /.github/
            /fantasy_football_data_scripts/
            /scripts/
            /requirements.txt
            /pyproject.toml

      - name: Setup Python
        id: setup-python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Restore canonical research public lake
        id: research-lake
        uses: ./.github/actions/restore-research-ops-cache
        with:
          cache-key: research-public-lake-v5-Linux-2000-plus-player-bio
          release-tag: ops-cache-v6-2000-plus-player-bio

      - name: Verify ops cache from research lake
        shell: bash
        run: |
          test -s research_public_lake/ops_cache.duckdb

      - name: Log cached ops cache
        run: |
          echo "[ops-cache] key-prefix=ops-cache-v5-${{ runner.os }}-manual-"
          echo "[research-lake] restore-hit=${{ steps.research-lake.outputs.cache-hit }}"
          echo "[ops-cache] OPS_CACHE_PATH=${OPS_CACHE_PATH}"
          test -s "${OPS_CACHE_PATH}" || {
            echo "::error::research_public_lake/ops_cache.duckdb is missing from the canonical research lake cache."
            exit 1
          }
          echo "[ops-cache] size-bytes=$(stat -c%s "${OPS_CACHE_PATH}")"
          echo "[ops-cache] size-human=$(du -h "${OPS_CACHE_PATH}" | cut -f1)"

      - name: Cache Python virtualenv
        uses: actions/cache@v5
        id: cache-venv
        with:
          path: .venv
          key: venv-${{ runner.os }}-py${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('requirements.txt') }}


      - name: Create virtualenv
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: python -m venv .venv

      - name: Install dependencies
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          source .venv/bin/activate
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Activate venv
        run: |
          echo "${{ github.workspace }}/.venv/bin" >> $GITHUB_PATH

      # ==================== PARSE INPUT ====================
      - name: Parse league data
        id: parse
        run: |
          source .venv/bin/activate
          python .github/scripts/resolve_sleeper_workflow_input.py
        env:
          LEAGUE_DATA_B64: ${{ github.event.inputs.league_data_b64 || github.event.client_payload.league_data_b64 }}
          USER_ID: ${{ github.event.inputs.user_id || github.event.client_payload.user_id }}
          IMPORT_MODE: full
          PRE_RESOLVED_DATABASE_NAME: ${{ needs.resolve-db-lock.outputs.database_name }}
          OUTPUT_JSON_PATH: league_data_input.json

      # ==================== SET DB_NAME AND DATA_DIR ====================
      - name: Set DB_NAME and DATA_DIR
        run: |
          echo "DB_NAME=${{ steps.parse.outputs.database_name }}" >> $GITHUB_ENV
          echo "DATA_DIR=${{ github.workspace }}/fantasy_football_data/sleeper_${{ steps.parse.outputs.database_name }}" >> $GITHUB_ENV

      # ==================== REGISTER LEAGUE (EARLY) ====================
      # Register league immediately so it's tracked even if import fails.
      - name: Register league (early)
        if: env.VERIFY_ONLY != 'true'
        env:
          SLEEPER_LEAGUE_ID: ${{ steps.parse.outputs.sleeper_league_id }}
          LEAGUE_NAME: ${{ steps.parse.outputs.league_name }}
          DATABASE_NAME: ${{ steps.parse.outputs.database_name }}
        run: |
          source .venv/bin/activate
          python - <<'PYEOF' || {
          import sys
          sys.path.insert(0, 'fantasy_football_data_scripts')
          import os

          from multi_league.utils.credential_store import register_league

          register_league(
              platform='sleeper',
              league_id=os.environ['SLEEPER_LEAGUE_ID'],
              league_name=os.environ['LEAGUE_NAME'],
              database_name=os.environ['DATABASE_NAME']
          )
          print("League registered (early)")
          PYEOF
            exit_code=$?
            if [ "$exit_code" -eq 139 ]; then
              echo "⚠️ DuckDB segfault on shutdown (exit 139) — league already registered, ignoring"
            else
              exit $exit_code
            fi
          }

      # ==================== CREATE DATA DIRECTORY ====================
      - name: Create data directory
        run: |
          mkdir -p "${{ env.DATA_DIR }}"/{league_settings,rosters,matchups,drafts,transactions}
          echo "Created data directory: ${{ env.DATA_DIR }}"
        id: datadir

      # ==================== CREATE SLEEPER CONTEXT ====================
      - name: Create Sleeper context
        env:
          LEAGUE_NAME: ${{ steps.parse.outputs.league_name }}
        run: |
          source .venv/bin/activate
          python - <<'PYEOF'
          import json
          import os
          from pathlib import Path
          from datetime import datetime
          import math

          with open("league_data_input.json") as f:
              league_data = json.load(f)

          import_mode = 'full'  # ALWAYS full - this is the full import worker
          data_dir = Path("fantasy_football_data/sleeper_${{ steps.parse.outputs.database_name }}").resolve()

          # Extract playoff settings
          playoff_teams = league_data.get('playoff_teams', 6)
          playoff_start_week = league_data.get('playoff_week_start') or league_data.get('playoff_start_week')

          # Calculate bye_teams
          if playoff_teams > 1:
              next_power_of_2 = 2 ** math.ceil(math.log2(playoff_teams))
              bye_teams = next_power_of_2 - playoff_teams
          else:
              bye_teams = 0

          # Detect H2H+Median
          standings_weights = league_data.get('standings_weights')
          uses_median_scoring = league_data.get('uses_median_scoring', False)
          if standings_weights and standings_weights.get('weights', {}).get('above_median_wins', 0) > 0:
              uses_median_scoring = True

          # Create Sleeper context
          context = {
              "league_id": "${{ steps.parse.outputs.sleeper_league_id }}",
              "league_name": os.environ["LEAGUE_NAME"],
              "database_name": "${{ steps.parse.outputs.database_name }}",
              "username": "${{ steps.parse.outputs.username }}",
              "sport": "nfl",
              "start_year": int("${{ steps.parse.outputs.start_year }}"),
              "end_year": int("${{ steps.parse.outputs.end_year }}"),
              "num_teams": league_data.get('num_teams'),
              "playoff_teams": playoff_teams,
              "bye_teams": bye_teams,
              "playoff_start_week": playoff_start_week,
              "regular_season_weeks": league_data.get('regular_season_weeks'),
              "uses_median_scoring": uses_median_scoring,
              "data_directory": str(data_dir),
              "max_workers": 3,
              "enable_caching": True,
              "rate_limit_per_min": 1000,
              "roster_to_manager": {},
              "roster_to_guid": {},
              "manager_name_overrides": league_data.get('manager_name_overrides', {}),
              "franchise_merges": league_data.get('franchise_merges', []),
              "external_column_maps": league_data.get('external_column_maps', []),
              "external_identity_maps": league_data.get('external_identity_maps', {}),
              "has_external_data": league_data.get('has_external_data', False),
              "merge_source": league_data.get('merge_source'),
              "merge_sources": league_data.get('merge_sources', []),
              "league_ids": league_data.get('league_ids', {}),
              "created_at": datetime.now().isoformat(),
              "updated_at": datetime.now().isoformat(),
              "platform": "sleeper",
              "import_mode": import_mode,
              "require_oauth": False,
              "keeper_rules": league_data.get('keeper_rules'),
              "league_rules": league_data.get('league_rules'),
              "standings_weights": standings_weights,
          }

          context_path = data_dir / "sleeper_context.json"
          with open(context_path, 'w') as f:
              json.dump(context, f, indent=2)

          with open("sleeper_context.json", 'w') as f:
              json.dump(context, f, indent=2)

          print(f"Sleeper context created: {context['start_year']}-{context['end_year']} (mode: {import_mode})")
          PYEOF

      # ==================== RUN SLEEPER IMPORT ====================
      - name: Run Sleeper import
        id: import
        env:
          AUTO_CONFIRM: "1"
        working-directory: fantasy_football_data_scripts
        run: |
          source ../.venv/bin/activate
          echo "========================================"
          echo "Starting Sleeper FULL import for ${{ steps.parse.outputs.league_name }}"
          echo "League ID: ${{ steps.parse.outputs.sleeper_league_id }}"
          echo "Historical Years: ${{ steps.parse.outputs.start_year }}-${{ steps.parse.outputs.end_year }}"
          echo "========================================"

          set +e
          python sleeper_initial_import.py \
            --context ../sleeper_context.json \
            --import-mode full \
            --skip-track-2-upload \
            2>&1 | tee ../import.log
          EXIT_CODE=${PIPESTATUS[0]}
          set -e

          echo "exit_code=${EXIT_CODE}" >> $GITHUB_OUTPUT

          if [ $EXIT_CODE -ne 0 ]; then
            echo "SLEEPER FULL IMPORT FAILED (exit code ${EXIT_CODE})"
            tail -200 ../import.log
            exit $EXIT_CODE
          fi

          echo "Sleeper full import completed successfully"

      # ==================== COUNT MATCHUP ROWS ====================
      - name: Count matchup rows
        if: steps.import.outputs.exit_code == '0'
        id: matchup_rows
        run: |
          source .venv/bin/activate
          python - <<'PYEOF'
          import os
          import sys
          from pathlib import Path

          import duckdb

          db_name = os.environ["DB_NAME"]
          data_dir = Path(os.environ["DATA_DIR"])
          db_path = data_dir / f"{db_name}.duckdb"

          if not db_path.exists():
              print(f"ERROR: Local DuckDB not found: {db_path}")
              sys.exit(1)

          conn = duckdb.connect(str(db_path), read_only=True)
          try:
              count = conn.execute("SELECT COUNT(*) FROM public.matchup").fetchone()[0]
          finally:
              conn.close()

          print(f"matchup: {count}")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
              fh.write(f"matchup_count={count}\n")
          PYEOF

      # ==================== CALCULATE EXPECTED RECORDS ====================
      - name: Calculate expected records
        if: steps.import.outputs.exit_code == '0' && steps.matchup_rows.outputs.matchup_count != '0'
        run: |
          cd fantasy_football_data_scripts
          python -m multi_league.transformations.matchup.expected_record_v2 \
            --db ${{ env.DB_NAME }} \
            --data-dir ${{ env.DATA_DIR }} \
            --n-sims 10000

      # ==================== CALCULATE PLAYOFF ODDS ====================
      - name: Calculate playoff odds
        if: steps.import.outputs.exit_code == '0' && steps.matchup_rows.outputs.matchup_count != '0'
        run: |
          cd fantasy_football_data_scripts
          python -m multi_league.transformations.matchup.playoff_odds_import \
            --db ${{ env.DB_NAME }} \
            --data-dir ${{ env.DATA_DIR }} \
            --n-sims 10000

      # Clutch equity now runs in-process inside playoff_odds_import (no separate step needed)

      # ==================== RUN AGGREGATIONS ====================
      - name: Run aggregations
        if: steps.import.outputs.exit_code == '0'
        run: |
          cd fantasy_football_data_scripts
          python ../scripts/refresh_aggregates.py \
            --db ${{ steps.parse.outputs.database_name }} \
            --data-dir ${{ env.DATA_DIR }}

      # ==================== UPLOAD LEAGUE DATA ====================
      - name: Upload league data
        if: steps.import.outputs.exit_code == '0' && env.VERIFY_ONLY != 'true'
        run: |
          cd fantasy_football_data_scripts
          python -c "
          import os, sys
          sys.path.insert(0, '.')
          from multi_league.core.local_db import LocalLeagueDB
          data_dir = os.environ['DATA_DIR']
          db_name = os.environ['DB_NAME']
          db = LocalLeagueDB(data_dir, db_name)
          db.connect()
          db.upload_to_fly(db_name)
          print('Upload to Fly complete')
          db.close()
          "

      # Rebuild derived tables from the merged canonical rows on Fly. The
      # delta publish preserves older year partitions, so this refresh keeps
      # historical canonical rows represented in matchup-derived and homepage
      # tables after every import.
      - name: Refresh centralized aggregates after merge
        if: steps.import.outputs.exit_code == '0' && env.VERIFY_ONLY != 'true'
        run: |
          cd fantasy_football_data_scripts
          python ../scripts/refresh_aggregates.py \
            --db "${{ steps.parse.outputs.database_name }}" \
            --data-dir "${{ github.workspace }}/fantasy_football_data/sleeper_${{ steps.parse.outputs.database_name }}"

      # ==================== REVALIDATE + WARM VERCEL CACHE ====================
      - name: Revalidate and warm Vercel cache
        if: steps.import.outputs.exit_code == '0' && env.VERIFY_ONLY != 'true'
        env:
          REVALIDATION_SECRET: ${{ secrets.REVALIDATION_SECRET }}
          DB_NAME: ${{ env.DB_NAME }}
        run: |
          python scripts/warm_vercel_cache.py \
            --db "$DB_NAME" \
            --secret "$REVALIDATION_SECRET" \
            --site-url "https://www.leaguehistory.app" \
            --mode full \
            --strategy expire \
            --jitter-seconds 0 \
            --warm-attempts 2 \
            --strict \
            --soft-warm-failures \
            --verify-hot

      # ==================== SUMMARY ====================
      - name: Print summary
        if: always()
        run: |
          echo "========================================"
          echo "SLEEPER FULL IMPORT SUMMARY"
          echo "========================================"
          echo "League: ${{ steps.parse.outputs.league_name }}"
          echo "Historical Years: ${{ steps.parse.outputs.start_year }}-${{ steps.parse.outputs.end_year }}"
          echo "Import exit code: ${{ steps.import.outputs.exit_code }}"
          echo "Import status: ${{ steps.import.outputs.exit_code == '0' && 'SUCCESS' || 'FAILED' }}"
          echo "========================================"
