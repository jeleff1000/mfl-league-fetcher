name: Audit every research matchup artifact from one manifest

on:
  push:
    paths:
      - 'audit_manifests/exact-audit-trigger.txt'
      - 'audit_manifests/exact-audit-retry.json'
  workflow_dispatch:
    inputs:
      since:
        description: UTC timestamp; inspect research artifacts created at or after this time
        required: true
        default: '2026-07-30T00:00:00Z'
      shard_count:
        description: Number of read-only artifact inspectors
        required: true
        default: '256'
      slice_index:
        description: Zero-based manifest slice for rate-limit-safe auditing
        required: true
        default: '0'
      slice_count:
        description: Number of sequential manifest slices
        required: true
        default: '1'
      resume_only_unverified:
        description: Audit only artifacts not verified by the prior exact audit
        required: true
        default: 'false'
      retry_artifact_ids:
        description: Comma-separated artifact IDs to retry; overrides the normal manifest scope
        required: false
        default: ''
      base_run_id:
        description: Prior audit run whose aggregate report should be repaired
        required: false
        default: ''

permissions:
  actions: read
  contents: read

env:
  # The private token is required for the cross-run artifact API.  Downloads
  # The inspector uses one direct archive request per artifact.
  GH_TOKEN: ${{ github.token }}
  SINCE: ${{ inputs.since || '2026-07-30T00:00:00Z' }}
  SHARD_COUNT: ${{ inputs.shard_count || '256' }}
  SLICE_INDEX: ${{ inputs.slice_index || '0' }}
  SLICE_COUNT: ${{ inputs.slice_count || '2' }}
  RESUME_ONLY_UNVERIFIED: ${{ inputs.resume_only_unverified || 'true' }}
  RETRY_ARTIFACT_IDS: ${{ inputs.retry_artifact_ids || '' }}
  BASE_RUN_ID: ${{ inputs.base_run_id || '' }}
  EVENT_NAME: ${{ github.event_name }}

jobs:
  manifest:
    if: ${{ github.event_name == 'workflow_dispatch' || startsWith(github.event.head_commit.message, '[exact-audit-slice') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    outputs:
      manifest_artifact: ${{ steps.manifest.outputs.name }}
      manifest_count: ${{ steps.manifest.outputs.count }}
      matrix: ${{ steps.manifest.outputs.matrix }}
      base_run_id: ${{ steps.manifest.outputs.base_run_id }}
    steps:
      - uses: actions/checkout@v4
      - id: manifest
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          python - <<'PY'
          import json, os
          from pathlib import Path

          source = json.loads(Path('audit_manifests/research_matchup_artifact_manifest_20260809.json').read_text())
          all_items = source['artifacts']
          all_ids = [int(item['artifact_id']) for item in all_items]
          if len(all_items) != 9479 or len(set(all_ids)) != len(all_ids):
              raise SystemExit(f"exact artifact manifest invalid: count={len(all_items)} unique={len(set(all_ids))}")
          slice_index = int(os.environ['SLICE_INDEX'])
          slice_count = int(os.environ['SLICE_COUNT'])
          retry_base_run_id = os.environ.get('BASE_RUN_ID', '')
          if os.environ['EVENT_NAME'] == 'push':
              slice_index = int(Path('audit_manifests/exact-audit-trigger.txt').read_text().strip())
              retry_config_path = Path('audit_manifests/exact-audit-retry.json')
              if retry_config_path.exists():
                  retry_config = json.loads(retry_config_path.read_text())
                  os.environ['RETRY_ARTIFACT_IDS'] = ','.join(str(value) for value in retry_config.get('artifact_ids', []))
                  os.environ['SHARD_COUNT'] = str(retry_config.get('shard_count', 1))
                  os.environ['SLICE_COUNT'] = str(retry_config.get('slice_count', 1))
                  slice_count = int(retry_config.get('slice_count', 1))
                  retry_base_run_id = str(retry_config.get('base_run_id', ''))
          if not 0 <= slice_index < slice_count:
              raise SystemExit(f"invalid slice {slice_index}/{slice_count}")
          retry_ids = {int(value.strip()) for value in os.environ['RETRY_ARTIFACT_IDS'].split(',') if value.strip()}
          if retry_ids:
              all_items = [item for item in all_items if int(item['artifact_id']) in retry_ids]
          elif os.environ['RESUME_ONLY_UNVERIFIED'].lower() == 'true':
              resume = json.loads(Path('audit_manifests/research_matchup_artifact_unverified_20260809.json').read_text())
              resume_ids = {int(item['artifact_id']) for item in resume['artifacts']}
              if len(resume_ids) != 5671:
                  raise SystemExit(f"unverified resume manifest invalid: {len(resume_ids)}")
              all_items = [item for item in all_items if int(item['artifact_id']) in resume_ids]
          # Balance by compressed artifact size rather than artifact-id modulo.
          # Modulo assignment can strand multi-GB snapshots in a few shards and
          # leave the exact-manifest audit appearing stalled.  This remains
          # deterministic and lossless: every manifest artifact is assigned to
          # exactly one bucket.
          buckets = [[] for _ in range(slice_count)]
          bucket_bytes = [0] * slice_count
          for item in sorted(all_items, key=lambda x: (-int(x.get('size_in_bytes', 0)), int(x['artifact_id']))):
              target = min(range(slice_count), key=lambda i: (bucket_bytes[i], len(buckets[i]), i))
              buckets[target].append(item)
              bucket_bytes[target] += int(item.get('size_in_bytes', 0))
          items = buckets[slice_index]
          ids = [int(item['artifact_id']) for item in items]
          manifest = {
              **source,
              'artifacts': items,
              'repo': '${{ github.repository }}',
              'since': os.environ['SINCE'],
              'shard_count': int(os.environ['SHARD_COUNT']),
              'artifact_count': len(items),
              'full_manifest_artifact_count': len(all_items),
              'slice_index': slice_index,
              'slice_count': slice_count,
          }
          Path('out/artifact_manifest.json').write_text(json.dumps(manifest, indent=2, sort_keys=True) + '\n')
          print(f'manifest artifacts={len(items)} bytes={sum(x["size_in_bytes"] for x in items)} exact_attachment_scope=true slice={slice_index}/{slice_count}')
          print(f"name=research-artifact-manifest-${{ github.run_id }}")
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as out:
              out.write(f"name=research-artifact-manifest-${{ github.run_id }}\n")
              out.write(f"count={len(items)}\n")
              out.write(f"matrix={json.dumps(list(range(int(os.environ['SHARD_COUNT']))), separators=(',', ':'))}\n")
              out.write(f"base_run_id={retry_base_run_id}\n")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: research-artifact-manifest-${{ github.run_id }}
          path: out/artifact_manifest.json
          if-no-files-found: error
          retention-days: 30
          compression-level: 0

  aggregate:
    needs: [manifest, inspect]
    if: ${{ always() && needs.manifest.result == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/download-artifact@v5
        with:
          pattern: research-artifact-audit-${{ github.run_id }}-*
          path: audit_parts
          # Preserve one directory per shard.  Flattening overwrites the
          # identically named artifact_audit.json files and corrupts the
          # aggregate audit.
          merge-multiple: false
          github-token: ${{ github.token }}
      - uses: actions/download-artifact@v5
        if: ${{ needs.manifest.outputs.base_run_id != '' }}
        with:
          name: research-artifact-audit-complete-${{ needs.manifest.outputs.base_run_id }}
          path: base_audit
          repository: ${{ github.repository }}
          run-id: ${{ needs.manifest.outputs.base_run_id }}
          github-token: ${{ github.token }}
      - uses: actions/download-artifact@v5
        with:
          name: research-artifact-manifest-${{ github.run_id }}
          path: manifest
      - name: Consolidate and validate complete artifact audit
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import csv, json
          from pathlib import Path
          parts = [json.loads(path.read_text()) for path in sorted(Path('audit_parts').rglob('artifact_audit.json'))]
          retry_records = [record for part in parts for record in part['results']]
          base_path = Path('base_audit/artifact_audit_complete.json')
          if base_path.exists():
              base_report = json.loads(base_path.read_text())
              manifest_count = int(base_report['manifest_artifacts'])
              by_id = {int(record['artifact_id']): record for record in base_report['records']}
              by_id.update({int(record['artifact_id']): record for record in retry_records})
              records = list(by_id.values())
              manifest_ids = set(by_id)
          else:
              scope = json.loads(Path('manifest/artifact_manifest.json').read_text())
              manifest_ids = {int(item['artifact_id']) for item in scope['artifacts']}
              manifest_count = len(manifest_ids)
              records = retry_records
          ids = [int(record['artifact_id']) for record in records]
          errors = [record for record in records if record.get('status') != 'inspected']
          duplicate_ids = sorted({artifact_id for artifact_id in ids if ids.count(artifact_id) > 1})
          missing_ids = sorted(manifest_ids - set(ids))
          out_of_scope_ids = sorted(set(ids) - manifest_ids)
          report = {
              'manifest_artifacts': manifest_count,
              'inspected_records': len(records),
              'inspection_errors': len(errors),
              'duplicate_artifact_ids': duplicate_ids,
              'missing_artifact_ids': missing_ids,
              'out_of_scope_artifact_ids': out_of_scope_ids,
              'complete': len(records) == manifest_count and not errors and not duplicate_ids and not missing_ids and not out_of_scope_ids,
              'records': sorted(records, key=lambda record: (record['artifact_id'], record.get('artifact', ''))),
          }
          Path('artifact_audit_complete.json').write_text(json.dumps(report, indent=2, sort_keys=True) + '\n')
          with Path('artifact_checklist.csv').open('w', newline='', encoding='utf-8') as handle:
              fields = ['artifact_id','artifact','workflow_run_id','created_at','artifact_status','file','file_type','kind','rows','column_count','columns','missing_canonical','forbidden_columns']
              writer = csv.DictWriter(handle, fieldnames=fields)
              writer.writeheader()
              for record in report['records']:
                  files = record.get('files') or [{}]
                  for file_record in files:
                      writer.writerow({
                          'artifact_id': record.get('artifact_id'),
                          'artifact': record.get('artifact'),
                          'workflow_run_id': record.get('workflow_run_id'),
                          'created_at': record.get('created_at'),
                          'artifact_status': record.get('status'),
                          'file': file_record.get('file'),
                          'file_type': file_record.get('type'),
                          'kind': file_record.get('kind'),
                          'rows': file_record.get('rows'),
                          'column_count': file_record.get('column_count'),
                          'columns': json.dumps(file_record.get('columns', []), separators=(',', ':')),
                          'missing_canonical': json.dumps(file_record.get('missing_canonical', []), separators=(',', ':')),
                          'forbidden_columns': json.dumps(file_record.get('forbidden_columns', []), separators=(',', ':')),
                      })
          print(json.dumps({k: report[k] for k in ('manifest_artifacts','inspected_records','inspection_errors','complete')}))
          if not report['complete']:
              raise SystemExit('complete artifact audit failed: every artifact was not successfully inspected')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: research-artifact-audit-complete-${{ github.run_id }}
          path: |
            artifact_audit_complete.json
            artifact_checklist.csv
          if-no-files-found: error
          retention-days: 30
          compression-level: 0

  inspect:
    needs: manifest
    strategy:
      fail-fast: false
      max-parallel: 15
      matrix:
        shard_index: ${{ fromJSON(needs.manifest.outputs.matrix) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 355
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install bounded inspection dependencies
        run: python -m pip install duckdb==1.5.1 pyarrow==15.0.2
      - uses: actions/download-artifact@v5
        with:
          name: research-artifact-manifest-${{ github.run_id }}
          path: manifest
      - name: Download and inspect assigned artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          python - <<'PY'
          import csv, json, os, random, shutil, tempfile, time, urllib.error, urllib.request, zipfile
          from pathlib import Path
          import duckdb

          manifest = json.loads(Path('manifest/artifact_manifest.json').read_text())
          shard = int('${{ matrix.shard_index }}')
          count = int(manifest['shard_count'])
          canonical = [
              'db_name','year','week','NFL_player_id','is_started','is_rostered',
              'fantasy_points','win','champion','clutch_equity','manager_lamar',
              'manager','team_points','final_playoff_seed','is_playoffs',
              'has_po_signal','player','position','fantasy_position','platform',
              'team_key','team_name','nfl_team_api','yahoo_player_id',
              'sleeper_player_id','espn_player_id','fleaflicker_player_id',
              'mfl_player_id','made_playoffs',
          ]
          # loss/tie are approved optional canonical player fields.  Keep only
          # genuinely forbidden source/legacy fields here.
          forbidden = {'opponent_points','is_championship','is_active','is_playoffs_bf','made_po_bf'}
          selected = [a for a in manifest['artifacts'] if int(a['artifact_id']) % count == shard]
          root = Path('work'); root.mkdir(exist_ok=True)
          results = []
          failures = []
          class NoAuthRedirect(urllib.request.HTTPRedirectHandler):
              def redirect_request(self, req, fp, code, msg, headers, newurl):
                  return urllib.request.Request(newurl, headers={'Accept': 'application/octet-stream'})
          opener = urllib.request.build_opener(NoAuthRedirect)
          for item in selected:
              rec = {k: item.get(k) for k in ('artifact_id','artifact','created_at','size_in_bytes','workflow_run_id')}
              rec['shard_index'] = shard
              try:
                  target = root / str(item['artifact_id'])
                  target.mkdir(parents=True, exist_ok=True)
                  request = urllib.request.Request(
                      item['archive_download_url'],
                      headers={
                          'Authorization': f"Bearer {os.environ['GH_TOKEN']}",
                          'Accept': 'application/vnd.github+json',
                          'X-GitHub-Api-Version': '2022-11-28',
                      },
                  )
                  archive = target / 'artifact.zip'
                  for attempt in range(8):
                      try:
                          with opener.open(request, timeout=300) as response, archive.open('wb') as handle:
                              shutil.copyfileobj(response, handle)
                          break
                      except urllib.error.HTTPError as exc:
                          if exc.code not in {403, 429, 500, 502, 503, 504} or attempt == 7:
                              raise
                          retry_after = exc.headers.get('Retry-After')
                          delay = float(retry_after) if retry_after else min(120.0, 10.0 * (2 ** attempt))
                          time.sleep(delay + random.uniform(0.0, 3.0))
                  with zipfile.ZipFile(archive) as bundle:
                      bundle.extractall(target / 'unzipped')
                  archive.unlink()
                  downloaded_files = [path for path in target.rglob('*') if path.is_file()]
                  rec['download_bytes'] = sum(path.stat().st_size for path in downloaded_files)
                  files = []
                  con = duckdb.connect()
                  for path in sorted(target.rglob('*')):
                      if not path.is_file():
                          continue
                      suffix = path.suffix.lower()
                      if suffix == '.parquet':
                          cols = [r[0] for r in con.execute('DESCRIBE SELECT * FROM read_parquet(?)', [str(path)]).fetchall()]
                          row_count = con.execute('SELECT COUNT(*) FROM read_parquet(?)', [str(path)]).fetchone()[0]
                          kind = 'exact_canonical_player' if cols == canonical else ('source_team_or_matchup' if {'db_name','year','week','team_key'} <= set(cols) else 'other_parquet')
                          files.append({'file': path.name, 'type': 'parquet', 'rows': row_count, 'columns': cols, 'column_count': len(cols), 'kind': kind, 'missing_canonical': sorted(set(canonical)-set(cols)), 'forbidden_columns': sorted(set(cols)&forbidden)})
                      else:
                          files.append({'file': path.name, 'type': suffix.lstrip('.') or 'no_extension', 'bytes': path.stat().st_size, 'kind': 'non_parquet'})
                  con.close()
                  rec['files'] = files
                  rec['status'] = 'inspected'
              except Exception as exc:
                  rec['status'] = 'inspection_error'
                  rec['error'] = f'{type(exc).__name__}: {exc}'
                  failures.append(rec)
              finally:
                  shutil.rmtree(root / str(item['artifact_id']), ignore_errors=True)
              results.append(rec)
          Path('out/artifact_audit.json').write_text(json.dumps({'manifest_artifacts':len(manifest['artifacts']), 'shard_index':shard, 'shard_count':count, 'selected_artifacts':len(selected), 'results':results, 'failures':failures}, indent=2, sort_keys=True)+'\n')
          with open('out/artifact_audit.csv','w',newline='',encoding='utf-8') as fh:
              fields=['artifact_id','artifact','created_at','size_in_bytes','download_bytes','status','error']
              writer=csv.DictWriter(fh, fieldnames=fields); writer.writeheader()
              for rec in results: writer.writerow({k:rec.get(k,'') for k in fields})
          for failure in failures[:25]:
              print(f"artifact_failure id={failure.get('artifact_id')} name={failure.get('artifact')} error={failure.get('error')}", flush=True)
          print(f'inspected={len(results)-len(failures)} errors={len(failures)} artifacts on shard {shard}/{count}')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: research-artifact-audit-${{ github.run_id }}-${{ matrix.shard_index }}
          path: out/*
          if-no-files-found: error
          retention-days: 30
          compression-level: 0
